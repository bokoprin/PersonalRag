<Window x:Class="Astra.Gui.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Astra.Gui"
 Title="PersonalRag" Width="1180" Height="760" MinWidth="780" MinHeight="420" FontFamily="Segoe UI, Yu Gothic UI" FontSize="13" Background="White" Loaded="OnLoaded" Closing="OnClosing" PreviewKeyDown="OnGlobalKey">
 <Window.Resources>
  <Style TargetType="TextBox"><Setter Property="Padding" Value="6,4"/><Setter Property="VerticalContentAlignment" Value="Center"/></Style>
  <Style TargetType="Button"><Setter Property="Padding" Value="9,3"/><Setter Property="Margin" Value="4,0,0,0"/></Style>
  <Style TargetType="ListViewItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="3"/></Style>
  <Style TargetType="GridViewColumnHeader"><Setter Property="HorizontalContentAlignment" Value="Left"/><Setter Property="Padding" Value="6,3"/></Style>
 </Window.Resources>
 <DockPanel>
  <Border DockPanel.Dock="Top" Background="#FBFBFB" BorderBrush="#D4D4D4" BorderThickness="0,0,0,1" Padding="10,8">
   <Grid>
    <Grid.ColumnDefinitions><ColumnDefinition Width="100"/><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/><ColumnDefinition Width="185"/></Grid.ColumnDefinitions>
    <Grid.RowDefinitions><RowDefinition Height="32"/><RowDefinition Height="5"/><RowDefinition Height="32"/></Grid.RowDefinitions>
    <TextBlock Text="ファイル名 / パス" VerticalAlignment="Center"/>
    <TextBox x:Name="FileQuery" Grid.Column="1" AutomationProperties.AutomationId="fileQuery" TextChanged="QueryChanged"/>
    <ComboBox x:Name="Scope" Grid.Column="2" SelectedIndex="0" Margin="8,0" VerticalContentAlignment="Center" SelectionChanged="QueryChanged"><ComboBoxItem Content="ファイル名"/><ComboBoxItem Content="フルパス"/></ComboBox>
    <CheckBox x:Name="CaseSensitive" Grid.Column="3" Content="大文字/小文字を区別" VerticalAlignment="Center" Checked="QueryChanged" Unchecked="QueryChanged"/>
    <TextBlock Grid.Row="2" Text="内容" VerticalAlignment="Center"/>
    <TextBox x:Name="ContentQuery" Grid.Row="2" Grid.Column="1" AutomationProperties.AutomationId="contentQuery" TextChanged="QueryChanged"/>
    <ComboBox x:Name="Mode" Grid.Row="2" Grid.Column="2" SelectedIndex="0" Margin="8,0" VerticalContentAlignment="Center" SelectionChanged="QueryChanged"><ComboBoxItem Content="Literal"/><ComboBoxItem Content="Regex"/><ComboBoxItem Content="Wildcard"/></ComboBox>
    <Button Grid.Row="2" Grid.Column="3" Content="内容をクリア" HorizontalAlignment="Left" Click="ClearContent"/>
   </Grid>
  </Border>
  <Border DockPanel.Dock="Top" BorderBrush="#D4D4D4" BorderThickness="0,0,0,1" Padding="10,5">
   <DockPanel><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="対象フォルダー" Click="ChooseFolder"/><Button x:Name="More" Content="さらに表示" Click="ShowMore" IsEnabled="False"/></StackPanel><TextBlock x:Name="Summary" Text="対象フォルダーを選択してください" VerticalAlignment="Center"/></DockPanel>
  </Border>
  <Border DockPanel.Dock="Bottom" Background="#F7F7F7" BorderBrush="#D4D4D4" BorderThickness="0,1,0,0" Padding="10,5">
   <DockPanel><TextBlock DockPanel.Dock="Right" Foreground="#777" Text="Ctrl+F: ファイル検索 / Ctrl+Shift+F: 内容検索 / 一覧↑↓: ファイル / 一致欄↑↓: ヒット"/><TextBlock x:Name="Status" Text="準備中"/></DockPanel>
  </Border>
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <ListView x:Name="Results" AutomationProperties.AutomationId="results" SelectionChanged="SelectedFileChanged" PreviewKeyDown="ResultKey" MouseDoubleClick="OpenSelected"
    VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.IsDeferredScrollingEnabled="True" ScrollViewer.HorizontalScrollBarVisibility="Auto">
    <ListView.View><GridView><GridViewColumn Header="Name" Width="230" DisplayMemberBinding="{Binding Name}"/><GridViewColumn Header="Path" Width="480" DisplayMemberBinding="{Binding Directory}"/><GridViewColumn Header="Size" Width="92" DisplayMemberBinding="{Binding Size}"/><GridViewColumn Header="Modified" Width="150" DisplayMemberBinding="{Binding Modified}"/><GridViewColumn x:Name="HitsColumn" Header="Hits" Width="0" DisplayMemberBinding="{Binding Hits}"/></GridView></ListView.View>
   </ListView>
   <Border x:Name="Preview" Grid.Row="1" Height="210" Visibility="Collapsed" Background="#FBFBFB" BorderBrush="#B8B8B8" BorderThickness="0,1,0,0">
    <DockPanel><Border DockPanel.Dock="Top" Background="#F7F7F7" Padding="10,5"><DockPanel><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="前の一致" Click="PreviousHit"/><Button Content="次の一致" Click="NextHit"/></StackPanel><TextBlock x:Name="PreviewTitle" VerticalAlignment="Center" FontWeight="SemiBold"/></DockPanel></Border>
     <ListBox x:Name="HitList" AutomationProperties.AutomationId="hits" PreviewKeyDown="HitKey" SelectionChanged="HitSelected" BorderThickness="0" Background="Transparent" VirtualizingPanel.IsVirtualizing="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
      <ListBox.ItemTemplate><DataTemplate><StackPanel Margin="6,3"><TextBlock Text="{Binding Location}" Foreground="#555" FontSize="12"/><local:HitTextBlock Hit="{Binding}" FontFamily="Consolas, Yu Gothic UI" TextWrapping="Wrap"/></StackPanel></DataTemplate></ListBox.ItemTemplate>
     </ListBox>
    </DockPanel>
   </Border>
  </Grid>
 </DockPanel>
</Window>
