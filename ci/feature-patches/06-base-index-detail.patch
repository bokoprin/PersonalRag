--- a/search-core/src/builder.rs
+++ b/search-core/src/builder.rs
@@ -207,6 +207,23 @@
     pub current_path: Option<PathBuf>,
 }
 
+#[derive(Clone, Copy, Debug, Default)]
+pub struct DiskPathBuildTimings {
+    pub hydration_wall: Duration,
+    pub hydration_read_work: Duration,
+    pub normalize_work: Duration,
+    pub segment_sample_work: Duration,
+    pub segment_core_work: Duration,
+    pub name_grams_work: Duration,
+    pub dedup_work: Duration,
+    pub content_grams_work: Duration,
+    pub content_post_work: Duration,
+    pub name_post_work: Duration,
+    pub segment_write_work: Duration,
+    pub acceleration_work: Duration,
+    pub manifest_write_wall: Duration,
+}
+
 #[derive(Clone, Debug)]
 pub struct DiskPathBuildReport {
     pub build: BuildReport,
@@ -220,4 +238,5 @@
     pub processed_files: usize,
     pub skipped_files: usize,
     pub bytes_read: u64,
+    pub timings: DiskPathBuildTimings,
 }
@@ -358,9 +377,57 @@
     }
 }
 
+#[derive(Default)]
+struct BuildTimingAccumulator {
+    hydration_read_ns: AtomicU64,
+    normalize_ns: AtomicU64,
+    segment_sample_ns: AtomicU64,
+    segment_core_ns: AtomicU64,
+    name_grams_ns: AtomicU64,
+    dedup_ns: AtomicU64,
+    content_grams_ns: AtomicU64,
+    content_post_ns: AtomicU64,
+    name_post_ns: AtomicU64,
+    segment_write_ns: AtomicU64,
+    acceleration_ns: AtomicU64,
+}
+
+fn elapsed_ns(started: Instant) -> u64 {
+    started.elapsed().as_nanos().min(u128::from(u64::MAX)) as u64
+}
+
+fn record_elapsed(counter: &AtomicU64, started: Instant) {
+    counter.fetch_add(elapsed_ns(started), Ordering::Relaxed);
+}
+
+fn accumulated_duration(counter: &AtomicU64) -> Duration {
+    Duration::from_nanos(counter.load(Ordering::Relaxed))
+}
+
+impl BuildTimingAccumulator {
+    fn snapshot(&self, hydration_wall_ns: u64, manifest_write_ns: u64) -> DiskPathBuildTimings {
+        DiskPathBuildTimings {
+            hydration_wall: Duration::from_nanos(hydration_wall_ns),
+            hydration_read_work: accumulated_duration(&self.hydration_read_ns),
+            normalize_work: accumulated_duration(&self.normalize_ns),
+            segment_sample_work: accumulated_duration(&self.segment_sample_ns),
+            segment_core_work: accumulated_duration(&self.segment_core_ns),
+            name_grams_work: accumulated_duration(&self.name_grams_ns),
+            dedup_work: accumulated_duration(&self.dedup_ns),
+            content_grams_work: accumulated_duration(&self.content_grams_ns),
+            content_post_work: accumulated_duration(&self.content_post_ns),
+            name_post_work: accumulated_duration(&self.name_post_ns),
+            segment_write_work: accumulated_duration(&self.segment_write_ns),
+            acceleration_work: accumulated_duration(&self.acceleration_ns),
+            manifest_write_wall: Duration::from_nanos(manifest_write_ns),
+        }
+    }
+}
+
 fn hydrate_disk_document<T: DiskPathSource>(
     root: &Path,
     canonical_root: &Path,
     source: &T,
     max_file_bytes: u64,
+    timings: Option<&BuildTimingAccumulator>,
 ) -> Option<DocumentInput> {
     let path = source.path();
     let size_bytes = match source.known_size_bytes() {
@@ -383,30 +450,20 @@
             .unwrap_or_else(|| path.to_path_buf());
         path_to_portable(&relative)
     };
-    let profile_build = profile_build_enabled();
     let normalized_content = if source.index_content() {
-        let read_started = profile_build.then(Instant::now);
+        let read_started = timings.map(|_| Instant::now());
         let mut content = fs::read(source.content_path()).ok()?;
-        if let Some(started) = read_started {
-            PROFILE_HYDRATION_READ_NS.fetch_add(
-                started.elapsed().as_nanos().min(u128::from(u64::MAX)) as u64,
-                Ordering::Relaxed,
-            );
+        if let (Some(timings), Some(started)) = (timings, read_started) {
+            record_elapsed(&timings.hydration_read_ns, started);
         }
-        let normalize_started = profile_build.then(Instant::now);
+        let normalize_started = timings.map(|_| Instant::now());
         content.make_ascii_lowercase();
-        if let Some(started) = normalize_started {
-            PROFILE_HYDRATION_NORMALIZE_NS.fetch_add(
-                started.elapsed().as_nanos().min(u128::from(u64::MAX)) as u64,
-                Ordering::Relaxed,
-            );
+        if let (Some(timings), Some(started)) = (timings, normalize_started) {
+            record_elapsed(&timings.normalize_ns, started);
         }
         content
     } else {
         Vec::new()
     };
-    let normalize_started = profile_build.then(Instant::now);
+    let normalize_started = timings.map(|_| Instant::now());
     let normalized_name = fold_ascii(display.as_bytes());
-    if let Some(started) = normalize_started {
-        PROFILE_HYDRATION_NORMALIZE_NS.fetch_add(
-            started.elapsed().as_nanos().min(u128::from(u64::MAX)) as u64,
-            Ordering::Relaxed,
-        );
+    if let (Some(timings), Some(started)) = (timings, normalize_started) {
+        record_elapsed(&timings.normalize_ns, started);
     }
     Some(DocumentInput::new(
         display.clone(),
@@ -408,5 +465,3 @@
 }
 
 static PROFILE_BUILD_ENABLED: OnceLock<bool> = OnceLock::new();
-static PROFILE_HYDRATION_READ_NS: AtomicU64 = AtomicU64::new(0);
-static PROFILE_HYDRATION_NORMALIZE_NS: AtomicU64 = AtomicU64::new(0);
 
@@ -439,5 +494,6 @@
     max_file_bytes: u64,
     workers: usize,
     cancel: Option<&AtomicBool>,
+    timings: Option<&BuildTimingAccumulator>,
     mut on_progress: F,
 ) -> Result<Vec<Option<DocumentInput>>>
@@ -487,4 +543,5 @@
                 canonical_root,
                 source,
                 max_file_bytes,
+                timings,
             ));
@@ -521,4 +578,5 @@
                         canonical_root,
                         source,
                         max_file_bytes,
+                        timings,
                     ));
@@ -612,6 +670,7 @@
         max_file_bytes,
         workers,
         None,
+        None,
         |_| {},
     )
 }
@@ -674,4 +733,5 @@
     build_workers: usize,
     durable: bool,
     retain_documents: bool,
+    timings: Option<&BuildTimingAccumulator>,
 ) -> Result<(ManifestEntry, Option<Vec<DocumentInput>>)> {
@@ -682,3 +742,6 @@
     let sample_started = Instant::now();
     let sample = sample_stats(&task.documents);
     let sample_ms = sample_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.segment_sample_ns.fetch_add((sample_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -696,4 +759,9 @@
     let collect_q2 = should_collect_q2_seed(acceleration, &task.documents);
     let base_started = Instant::now();
-    let mut data = build_segment_data_slice_impl(&task.documents, task.doc_base, kind, collect_q2)?;
+    let mut data = build_segment_data_slice_impl(
+        &task.documents, task.doc_base, kind, collect_q2, timings,
+    )?;
     let base_ms = base_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.segment_core_ns.fetch_add((base_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -706,2 +774,5 @@
     let written = write_segment(&path, &data, durable)?;
     let base_write_ms = base_write_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.segment_write_ns.fetch_add((base_write_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -736,5 +807,8 @@
     let accel_ms = if acceleration == AccelerationProfile::None {
         0.0
     } else {
         accel_started.elapsed().as_secs_f64() * 1000.0
     };
+    if let Some(timings) = timings {
+        timings.acceleration_ns.fetch_add((accel_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -1001,8 +1075,5 @@
     fs::create_dir_all(output_dir)?;
     let started = Instant::now();
     let profile_build = profile_build_enabled();
-    if profile_build {
-        PROFILE_HYDRATION_READ_NS.store(0, Ordering::Relaxed);
-        PROFILE_HYDRATION_NORMALIZE_NS.store(0, Ordering::Relaxed);
-    }
+    let timings = BuildTimingAccumulator::default();
     let mut hydration_wall_ns = 0u64;
@@ -1037,4 +1108,5 @@
                             build_workers,
                             durable,
                             retain_documents,
+                            Some(&timings),
                         );
@@ -1093,3 +1165,4 @@
                     scan_workers,
                     cancel,
+                    Some(&timings),
                     |hydration| {
@@ -1198 +1271,3 @@
-    write_manifest(output_dir, options.mode, total_docs, &entries, durable)?;
+    let manifest_started = Instant::now();
+    write_manifest(output_dir, options.mode, total_docs, &entries, durable)?;
+    let manifest_write_ns = elapsed_ns(manifest_started);
@@ -1224,7 +1299,7 @@
             "BUILD_HYDRATION source_files={} docs={} read_ms={:.3} normalize_ms={:.3} wall_ms={:.3}",
             source_files,
             total_docs,
-            PROFILE_HYDRATION_READ_NS.load(Ordering::Relaxed) as f64 / 1_000_000.0,
-            PROFILE_HYDRATION_NORMALIZE_NS.load(Ordering::Relaxed) as f64 / 1_000_000.0,
+            timings.hydration_read_ns.load(Ordering::Relaxed) as f64 / 1_000_000.0,
+            timings.normalize_ns.load(Ordering::Relaxed) as f64 / 1_000_000.0,
             hydration_wall_ns as f64 / 1_000_000.0,
         );
@@ -1242,3 +1317,4 @@
             skipped_files: progress.skipped_files,
             bytes_read: progress.bytes_read,
+            timings: timings.snapshot(hydration_wall_ns, manifest_write_ns),
         },
@@ -1305,4 +1381,5 @@
             1,
             self.durable,
             false,
+            None,
         )?;
@@ -1438,6 +1515,7 @@
     let mut data = build_segment_data_slice_impl(
         segment_docs,
         begin,
         kind,
         should_collect_q2_seed(acceleration, segment_docs),
+        None,
     )?;
@@ -1507,4 +1585,5 @@
     doc_base: usize,
     kind: BuilderKind,
     collect_q2: bool,
+    timings: Option<&BuildTimingAccumulator>,
 ) -> Result<SegmentData> {
@@ -1532,2 +1611,5 @@
     }
     let name_grams_ms = phase_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.name_grams_ns.fetch_add((name_grams_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -1569,2 +1651,5 @@
     }
     let dedup_ms = phase_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.dedup_ns.fetch_add((dedup_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -1640,2 +1725,5 @@
     }
     let content_grams_ms = phase_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.content_grams_ns.fetch_add((content_grams_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -1653,2 +1741,5 @@
     };
     let content_post_ms = phase_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.content_post_ns.fetch_add((content_post_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
@@ -1667,2 +1758,5 @@
     };
     let name_post_ms = phase_started.elapsed().as_secs_f64() * 1000.0;
+    if let Some(timings) = timings {
+        timings.name_post_ns.fetch_add((name_post_ms * 1_000_000.0) as u64, Ordering::Relaxed);
+    }
--- a/search-core/src/lib.rs
+++ b/search-core/src/lib.rs
@@ -19,4 +19,4 @@
 pub use builder::{
     BuildMode, BuildOptions, BuildReport, BuildTuning, DiskPathBuildConfig, DiskPathBuildProgress,
-    DiskPathBuildReport, DiskPathInput, build_disk_corpus, build_disk_corpus_parallel,
+    DiskPathBuildReport, DiskPathBuildTimings, DiskPathInput, build_disk_corpus, build_disk_corpus_parallel,
